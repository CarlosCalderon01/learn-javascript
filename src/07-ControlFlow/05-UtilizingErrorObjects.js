/**
    Utilizing Error Objetc

    Los objetos de errores sirven para manejar y generar ecxepciones en su codigo

**/

/**

    1 AggregateError -->
        el AggregateErrorobjeto se utiliza para representar múltiples errores como un solo error.
        Se usa comúnmente en código asincrónico para capturar y manejar múltiples errores
        que pueden ocurrir durante operaciones paralelas o concurrentes.

    2 EvalError -->
        el EvalErrorobjeto no se utiliza con mucha frecuencia.
        Representa un error que ocurre al utilizar la eval()función para evaluar código JavaScript.
        Es raro encontrar este error en JavaScript moderno.

    3 InternalError -->
        el InternalErrorobjeto representa un error que se produce cuando ocurre un error interno en el motor de JavaScript. Es poco probable que encuentre este error en el desarrollo normal de JavaScript.

    4 RangeError -->
        el RangeErrorobjeto representa un error que ocurre cuando
        un valor numérico está fuera del rango válido. Por ejemplo,
        se produce cuando se usa Number.toFixed()con demasiados decimales.

    5 ReferenceError -->
        El ReferenceErrorobjeto representa un error que ocurre
        al intentar acceder a una variable u objeto que no está definido.
        Esto ocurre comúnmente cuando se hace referencia a
        una variable que está fuera de alcance.

    6 SyntaxError -->
        el SyntaxErrorobjeto representa un error en la sintaxis del código.
        Se genera cuando escribes código que no se ajusta a
        las reglas de sintaxis del lenguaje. Esto podría incluir
        puntos y coma faltantes, paréntesis que no coinciden u otros problemas de sintaxis.

**/

/**

    1 AggregateError -->

    2 EvalError --> ignorarlos

    3 InternalError --> informarlos

    4 RangeError -->

    5 ReferenceError -->

    6 SyntaxError -->

**/

/**

    1 AggregateError -->

**/

/**

    4 RangeError -->

**/

/**

    5 ReferenceError -->

**/

/**

    6 SyntaxError -->

**/
